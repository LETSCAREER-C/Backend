spring:
  datasource:
    url: jdbc:h2:mem:db;MODE=MYSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false
    generate-ddl: true
  sql:
    init:
      data-locations: classpath:data_ho0.sql
      mode: always
logging:
  level:
    org.hibernate.SQL: debug

---

cloud:
  aws:
    s3:
      bucket: letmec-static-files
    credentials:
      access-key: AKIA5CBDRIFCYAQHDFFD
      secret-key: Jzh1aRrwcAsdfdGtDzfwefdf079rbWOWdDQHI2rpPXDWl
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false